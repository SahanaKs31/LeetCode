class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        for item in range(len(nums)):
            for item1 in range(item+1,len(nums)):
                if nums[item]+nums[item1]==target:
                    return(item,item1)
                
