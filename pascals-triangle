class Solution:
    def generate(self, numRows: int) -> List[List[int]]:
        pas=[]
        for i in range(numRows):
            row=[1]*(i+1)
            for j in range(1,i):
                row[j]=pas[i-1][j-1]+pas[i-1][j]
            pas.append(row)
        return pas
