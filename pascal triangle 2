class Solution:
    def getRow(self, rowIndex: int) -> List[int]:
        pas=[]
        for i in range(rowIndex+1):
            row=[1]*(i+1)
            for j in range(1,i):
                row[j]=pas[i-1][j-1]+pas[i-1][j]
            pas.append(row)
        return pas[rowIndex]
